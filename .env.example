# Installed builds default to the live, authentication-only screen. Set this
# explicit development override to use local report fixtures.
DATA_SOURCE=mock

# The five non-secret NetSuite Public Client values are bundled in the Electron
# main process. Never add access tokens, refresh tokens, a client secret, or
# private keys to this or any other environment file.
